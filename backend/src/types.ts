export interface UserSignup {
    firstName: string
    lastName: string
    email: string
    password: string
}
export interface CreateEvent {
    name: string
    title: string
    description: string
    location: string
    creatorId: string
    eventDate?: Date
}

export interface User {
    
}